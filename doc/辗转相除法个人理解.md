# 辗转相除法个人理解

标签：算法

---

# 原理理解

```
答：
x y 的 最大公约数N

设x和y的最大公约数是N，所以x=aN,y=a'N

满足x+b=y
aN+b=a'N
b=(a'-a)N 按照乘法分配律 b可以被N整除（a'-a是整数）

分析：所以x b y 都可以被N整除（必须条件），于是求最简问题，缩小范围
注意最大公约数均满足 x b y 都可以被N整除，所以需满足Min(x,b,y)中最小的两者作为new x y辗转相除法递归找到能满足条件的值

满足条件：当x y时x%y==0时，说明y为x和y的最大除数，即为最大公约数
```


